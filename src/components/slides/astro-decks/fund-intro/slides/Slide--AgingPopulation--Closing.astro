---
---

<section data-transition="fade">
  <div class="slide-content ap-closing">
    <p class="ap-eyebrow">Join Us</p>
    <h2 class="ap-headline">Capture the $7T+ Longevity Economy</h2>
    <p class="ap-subtitle">Demographic inevitability meets scientific readiness</p>
  </div>
</section>

<style>
  .ap-closing {
    gap: 1.25rem;
  }

  .ap-eyebrow {
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.35em;
    text-transform: uppercase;
    color: var(--color-lilac);
    opacity: 0.75;
    margin: 0;
  }

  .ap-headline {
    font-size: clamp(2rem, 4.5vw, 4rem);
    font-weight: 800;
    line-height: 1.05;
    margin: 0;
    color: var(--color-alabaster);
    max-width: 60rem;
  }

  .ap-subtitle {
    font-size: clamp(1.05rem, 1.8vw, 1.5rem);
    margin: 0;
    color: var(--color-alabaster);
    opacity: 0.7;
    max-width: 48rem;
  }
</style>
